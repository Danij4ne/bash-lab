
# What is sudo?
# It means "superuser do".
# It allows you to execute a command with administrator privileges.


sudo apt update        # updates the system package list
sudo apt install git   # installs the git program
sudo nano /etc/hosts   # opens the config file with root permissions
