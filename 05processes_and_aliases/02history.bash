
history   # command history that shows the commands we have executed

!10       # runs the command that is in position 10 in the history list

!ls       # executes the most recent command in the history that started with "ls"

