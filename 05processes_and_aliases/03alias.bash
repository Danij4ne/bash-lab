
 alias # used to create a short name for a long command

ls -l
# lists files showing detailed information

alias ll='ls -l'
# creates an alias called ll that runs the ls -l command

ll
# executes the command associated with the ll alias (ls -l)

unalias ll
# removes the ll alias
