
# tree

tree          # shows a tree of directories and files

tree -a       # -a = hidden files, shows all files and directories including hidden ones

find          # used to find files by name or by criteria

find . -name "README.md"   # search by -name (name) README.md

find . -name "READ*"       # everything that starts with READ

























