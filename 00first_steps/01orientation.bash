pwd      # know where you are located

ls       # used to list the files and folders inside the directory you are in

ls -l    # shows complete information about the files, including permissions

ls -a    # shows hidden files

ls -lh   # also shows complete information but in a more abbreviated/human-readable format

ls /     # lists the contents of the root directory of the system, showing all main Linux folders

